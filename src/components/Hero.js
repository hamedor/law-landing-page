import React from "react";
import {hero, hero__title} from '../styles/Hero.module.css'

const Hero = () => {
    return(
        <div className={hero}>
            <h1 className={hero__title}>Page Title</h1>
        </div>
    )

}

export default Hero;

